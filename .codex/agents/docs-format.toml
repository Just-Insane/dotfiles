name = "docs-format"
description = "Low-cost documentation and formatting consistency role for an approved change."
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = "Check documentation and formatting consistency within the approved scope. Respect repository validation policy and do not run prohibited local checks."
